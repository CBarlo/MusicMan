# ── Expedition data (personal, never commit) ──
config.yaml
config.bak
install.yaml

# ── Media assets (personal + copyright; live on the device, not in the repo) ──
assets/

# ── Loose media files ──
*.mov
*.mp4
*.mp3
*.wav
*.webm
*.jpg
*.jpeg
*.png
*.gif
*.webp
# ...but allow documentation images in pole_node/
!pole_node/*.png
!pole_node/*.jpg
!pole_node/*.svg

# ── Runtime files ──
logs/
games.json
*.tmp
*.corrupt
*.zip
backups/
good vibes only*/

# ── Python ──
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/

# ── macOS ──
.DS_Store
.claude/
